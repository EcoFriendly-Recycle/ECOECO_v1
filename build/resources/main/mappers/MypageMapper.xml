<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recycle.ecoeco.user.mypage.model.dao.MypageMapper">

    <select id="selectUserById" resultType="String">
        SELECT
            userId
        FROM
            user_info
        WHERE
            userId = #{ userid }
    </select>

    <insert id="joinus" parameterMap="com.recycle.ecoeco.user.mypage.model.dto.UserInfoDTO">
        INSERT INTO
            user_info (user_id, user_pwd, user_name, user_pnumber, user_email, user_birth, user_gender)
        VALUES (#{userId}, #{userPwd}, #{userName}, #{userPnumber}, #{userEmail}, #{userBirth}, #{userGender});
    </insert>

</mapper>